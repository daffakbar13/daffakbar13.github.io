import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#172153',
    },
    secondary: {
      main: '#f56f46',
    },
  },
})

export default theme
